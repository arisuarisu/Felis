# Feline Information System

Start frontend:
cd frontend && npm install && npm start

Start backend:
cd backend && npm install && npx nodemon server.js

Backend and frontend should run simultaneously.
